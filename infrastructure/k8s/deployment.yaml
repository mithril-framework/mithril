apiVersion: apps/v1
kind: Deployment
metadata:
  name: mithril
  namespace: mithril
  labels:
    app: mithril
spec:
  replicas: 1
  selector:
    matchLabels:
      app: mithril
  template:
    metadata:
      labels:
        app: mithril
    spec:
      containers:
        - name: app
          image: ghcr.io/REPO_OWNER/mithril:latest
          imagePullPolicy: Always
          ports:
            - containerPort: 4000
              name: http
          envFrom:
            - configMapRef:
                name: mithril-config
            - secretRef:
                name: mithril-secret
          resources:
            requests:
              memory: "128Mi"
              cpu: "100m"
            limits:
              memory: "512Mi"
              cpu: "500m"
          livenessProbe:
            httpGet:
              path: /health
              port: 4000
            initialDelaySeconds: 15
            periodSeconds: 10
            timeoutSeconds: 3
            failureThreshold: 3
          readinessProbe:
            httpGet:
              path: /health
              port: 4000
            initialDelaySeconds: 5
            periodSeconds: 5
            timeoutSeconds: 3
            failureThreshold: 2
