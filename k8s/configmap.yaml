apiVersion: v1
kind: ConfigMap
metadata:
  name: mithril-config
  namespace: mithril
data:
  APP_ENV: "production"
  APP_DEBUG: "false"
  APP_PORT: "4000"
  DB_PORT: "5432"
  REDIS_PORT: "6379"
  LOG_LEVEL: "info"
  # Add other non-sensitive configuration here
