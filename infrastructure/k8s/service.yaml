apiVersion: v1
kind: Service
metadata:
  name: mithril
  namespace: mithril
  labels:
    app: mithril
spec:
  type: ClusterIP
  ports:
    - port: 80
      targetPort: 4000
      protocol: TCP
      name: http
  selector:
    app: mithril
